#include "libft.h"


static function_name(char c)
{
	
}

char	**ft_split(char const *s, char c)
{
	int	i;
	char	*result[];

	i = 0;
	if (!s)
		return (NULL);
	ft_strtrim(s, c)
	*result = (char *)malloc(sizeof(int) * 10);
	if (!result)
		return (NULL);
	while ()
	return (result);
}
